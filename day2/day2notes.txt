type checking  and type safety 
any and unknown 
enum 
Enum in typescript are commonly used when we want to represent 
a set of related values and choose one value from multiple options.

Enum provide a convinent way to define set of named values and associate
them with a specific meaning .

value with from 0 by default 